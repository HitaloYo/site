<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Sobre Nós - Instituto Qualquer</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet" />
  <%= stylesheet_link_tag "sobre" %>
</head>
<body>
  <header>
    <h1>Instituto Qualquer</h1>
    <div class="header-buttons">
     <%= link_to root_path, class: "header-button" do %>
      <button><i class="bi bi-house-door"></i> Página Principal</button>
     <% end %>
     <%= button_to logout_path, method: :delete, class: 'header-button logout-btn', form: { style: 'display: inline;' } do %>
     <button_to><i class="bi bi-box-arrow-right"></i> Sair</button>
     <% end %>
    </div>
    <%= image_tag "logo.png", alt: "Logo do Instituto", class: "logo" %>
  </header>

  <main>
    <%= yield %>
  </main>
</body>
</html>